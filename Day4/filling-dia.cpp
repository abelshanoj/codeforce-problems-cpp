#include <bits/stdc++.h>
using namespace std;
/*Find the number of different coverings possible
2 coverings are different if 2 triangles are filled by the same diamond shape in one covering and different diamond shape in another one.*/
main()
{
 int T;
 cin >> T;
 while (T--)
 {
  int n;
  cin >> n;
  cout << n << endl;
 }
}